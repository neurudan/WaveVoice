{
    "name": "val_loss",
    "method": "bayes",
    "metric": {
        "name": "val_loss",
        "goal": "minimize"
    },
    "early_terminate": {
        "type": "envelope"
    },
    "parameters": {
        "config_name": {
            "value": "default.json"
        },

        
        "01.SWEEP.MODEL.num_filters": {
            "values": [
                32,
                64,
                128,
                256
            ]
        }
    }
}